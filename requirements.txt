# Core testing
pytest==8.4.2
jsonschema==4.26.0

# Document parsing
pypdf==5.9.0
python-docx==1.2.0

# HTTP client (for model API calls)
requests==2.34.2

# UI automation (screenshots)
playwright==1.62.0

# Chinese tokenization (optional)
jieba==0.42.1

# Flask for Vercel serverless
Flask>=3.0,<4.0

# Persistent PostgreSQL store (Neon) for accounts/history in production
psycopg[binary]>=3.1,<4
itsdangerous>=2.2,<3.0

# Optional model provider enabled by ZHIPU_API_KEY
zhipuai>=2.0,<3.0

