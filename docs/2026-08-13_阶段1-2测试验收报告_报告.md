---
date: "2026-08-13"
type: "测试验收报告"
phase: "阶段 1-2（M1）"
tags: ["职业教练", "测试验收", "QuickDemo", "OCR", "上传进度"]
---

# 职业教练 · 阶段 1–2 测试验收报告（第一次）

## 原始任务

- 在第一轮竞品驱动迭代（阶段 0：真实账号体系 + Neon Postgres 持久化 + 历史记录侧边栏）完成后，按规划执行阶段 1–2：Quick Demo 一键体验、扫描件 OCR 兜底与错误路径、上传进度与失败态，并在收尾时输出第一次测试验收报告（Obsidian Markdown + 桌面 DOCX 双格式）。

## 核心摘要

- 阶段 1–2 全部功能完成并纳入自动回归：未登录游客可一键体验 F1/F2（Quick Demo，明确标注演示数据）；扫描件 PDF 返回明确的 scanned_pdf 错误而非静默空文本；OCR key 可配置（mock 验证）后图片型 PDF 可提取文本；前端上传带进度回调与失败错误态。部署源三树（docs / public / ui-prototype）保持一致。

## 验证范围与结果

- Quick Demo：游客点击一键体验后，无需上传即可看到 F1 诊断与 F2 匹配结果，演示结果带明确标注（data-bridge 仅在 ?demo=1 时返回合成数据，正常模式不伪造）。
- OCR 兜底：扫描件 PDF 上传返回 scanned_pdf 明确错误；配置 OCR key（mock 验证）后图片型 PDF 可提取文本。
- 上传体验：uploadResumeWithProgress / uploadJDWithProgress 带进度回调、超时与错误码透传；页面展示进度元素与错误映射。
- 自动测试：pytest 基线 333 通过（含新增 OCR provider 用例）；Node 契约 27 通过（含 quick-demo / upload-progress 契约）。
- 公网部署：GitHub Pages 与 Vercel 双端已部署（阶段 0 完成），阶段 1–2 功能在线上入口可见；收尾推送后再次验证。

## 验收标准达成情况

- ✅ 未登录游客点击一键体验后，无需上传即可看到 F1 诊断与 F2 匹配结果。
- ✅ 演示结果带明确标注。
- ✅ 扫描件 PDF 上传返回 scanned_pdf 明确错误，不产生空文本静默成功。
- ✅ 配置 OCR key（mock 验证）后图片型 PDF 可提取文本。
- ✅ 前端上传进度与失败错误态可用。
- ✅ 相关 pytest / Node 全绿。

## 行动项

- 阶段 3 任务中心：客户端驱动分片 + 幂等 + 前端进度（已在第二阶段验收中完成）。
- 真实 OCR/ASR/模型 key 接入属外部依赖项，配置后激活真实服务，不影响当前功能可用性。
