---
date: "2026-08-13"
type: "测试验收报告"
phase: "阶段 3-5（M2-M4）"
tags: ["职业教练", "测试验收", "任务中心", "SSE", "RAG", "优化器", "工程化", "投递闭环"]
---

# 职业教练 · 阶段 3–5 全量测试验收报告（第二次）

## 原始任务

- 在阶段 1–2 验收通过后，按既定规划依次完成：阶段 3 任务中心（客户端驱动分片、幂等、游客隔离）、阶段 4 内容生态（面经知识库 BM25、ASR provider 抽象、SSE 流式面试与快照恢复、简历优化器）、阶段 5 工程化（services 分层、provider adapter + mock-first、投递闭环 wf07）。全量回归通过后输出第二次测试验收报告（Obsidian Markdown + 桌面 DOCX 双格式），并按收尾口令提交推送。

## 核心摘要

- 阶段 3–5 全部落地：任务中心支持创建/查询/分片推进/幂等/游客隔离，F2 模式 B 走分片任务、模式 A 直连；面经知识库 24 条种子 × 7 分类 + BM25 检索；ASR 抽象默认 mock 不阻断主流程；F3 追问以 SSE 流式逐字渲染并支持 sessionStorage 快照恢复；简历优化器输出带「待确认」标记、用户确认后才落库；工程化上业务编排收敛到 services/ 五模块、MODEL_PROVIDER 支持 mock-first、wf07 投递闭环（求职信生成 → 人工确认 → 申请跟踪 CRUD）全链路可用。

## 验证范围与结果

- 阶段 3 任务中心：pytest 338 / Node 31 全绿。新增 tests/test_tasks.py（创建、幂等、分片推进、游客隔离）与 tests/test_tasks_contract.js（data-bridge 任务封装契约）。
- 阶段 4 内容生态：pytest 349 / Node 37 全绿。新增 tests/test_phase4.py（知识库分类/BM25 检索、mock ASR、SSE 流、优化器规则降级、apply-rewrite 落库）与 tests/test_phase4_contract.js（kb/f3/optimizer 前端契约）。
- 阶段 5 工程化：pytest 358 / Node 41 全绿。services/diagnosis_service、match_service、interview_service、task_service、apply_service 承担业务编排；tools/providers/model.py 提供 MODEL_PROVIDER（auto/mock）；wf07 覆盖求职信生成、申请保存/列表/删除与跨游客隔离。
- 全量回归：pytest 358 通过（基线 325 之上只增不减）；Node 契约 41 通过。
- 三树镜像：docs / public / ui-prototype 一致，tests/test_publish_mirror.js 校验通过（含阶段 4-5 新增文件）。

## 验收标准达成情况

- ✅ 全量回归不低于现有基线（358 ≥ 325，且只增不减）。
- ✅ 无任何 key 时 pytest 与 Node 契约全绿（mock-first 生效：模型、ASR、检索均可降级）。
- ✅ 求职信生成 → 人工确认 → 申请跟踪落库全链路可用（wf07，含权限隔离）。
- ⬜ Vercel 生产健康检查：收尾推送后线上验证（见交付说明）。

## 遗留项与外部依赖（不影响项目使用）

- 真实模型 7×3 复测：需配置 ZHIPU_API_KEY 等模型密钥后执行，当前以规则/BM25/mock 交付并全链路可测。
- Embedding 全量召回：配置 EMBEDDING_API_KEY 后由 BM25 升级为向量召回（接口已预留 notice 提示）。
- 真实 OCR / ASR 供应商：OCR key / ASR_PROVIDER（baidu/dashscope）配置后激活，未配置时以明确错误与文字回退兜底。

## 行动项

- 按收尾口令提交推送本会话真实代码改动（排除密钥/日志/临时文件），推送后验证 GitHub Pages 与 Vercel 线上功能。
- 生产健康检查通过后，将规划文档 6.4 最后一项验收标记为完成。
- 后续迭代可基于竞品报告优先级：真实模型复测、embedding 召回、投递闭环增强（岗位库/状态流转）。
